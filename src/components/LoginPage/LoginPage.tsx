import React from 'react'

interface Props {}

function LoginPage(props: Props) {
    const {} = props

    return (
        <div>
            <h1>Login</h1>
        </div>
    )
}

export default LoginPage;
